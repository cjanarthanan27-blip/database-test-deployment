{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python manage.py collectstatic --noinput && python manage.py migrate --noinput && gunicorn rathinamHR.wsgi --log-file -",
        "restartPolicyType": "ON_FAILURE",
        "healthCheckPath": "/admin/login/"
    }
}